# Conventions pessoais deste workspace

Copie este arquivo para `user__index.md` (gitignored) e liste aqui suas próprias conventions, uma
linha `@` por arquivo. Cada uma delas também usa o prefixo `user__` e vive em
`.claude/conventions/`.

@.claude/conventions/user__minha-convencao.md
